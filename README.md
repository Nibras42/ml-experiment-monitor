# ml-experiment-monitor
MLMonitor is a Django MLOps dashboard for logging ML training runs, tracking metrics live via WebSockets, comparing experiments, defining data pipelines, and receiving threshold alerts. A companion Python SDK lets any training script push metrics in 3 lines.
